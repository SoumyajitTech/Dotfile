# Static setup
#
# 5 desktops, all using the static names.
# Nothing will be appended, removed, or renamed.

min = 5
remove-empty = true
append-when-occupied = false
renamers = ["static"]

[names]
static = ["1", "2", "3", "4", "5"]
